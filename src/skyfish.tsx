import * as React from 'react';
import * as ReactDom from 'react-dom';
import Skyfish from './components/Skyfish';

const root = document.getElementById('skyfish-xr');

ReactDom.render(<Skyfish />, root);